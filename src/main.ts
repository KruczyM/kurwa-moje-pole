import './style.css';import './ui-additions.css';import './preview.css';import {Game} from './game/Game';import {CharacterPreview} from './game/ui/CharacterPreview';import type {EffectId} from './game/effects/EffectManager';
if(new URLSearchParams(location.search).get('debug')==='animations'){location.replace('/animation-diagnostics.html')}else{let game:Game|undefined;const names=['Amper','Antena','Gruczoł','Klątwa','Krwiak','Pień','Pierścień','Zawór'],preview=new CharacterPreview(document.querySelector<HTMLElement>('#character-preview-layer')!);let selected=names[0];preview.show(selected);const selection=document.querySelector<HTMLElement>('#character-select')!;names.forEach(name=>{const b=document.createElement('button');b.textContent=name;b.className=name===selected?'selected':'';b.onclick=()=>{selected=name;preview.show(name);selection.querySelectorAll('button').forEach(x=>x.classList.toggle('selected',x.textContent===name))};selection.append(b)});document.querySelector<HTMLButtonElement>('#play')!.onclick=()=>{preview.dispose();localStorage.setItem('camp-player-character',selected);document.querySelector<HTMLElement>('#start')!.hidden=true;game=new Game();game.canvas.requestPointerLock();game.start()};document.querySelector<HTMLButtonElement>('#resume')!.onclick=()=>game?.setPause(false);document.querySelector<HTMLButtonElement>('#close-dialog')!.onclick=()=>game?.closeDialog();document.querySelector<HTMLButtonElement>('#inspect-close')!.onclick=()=>game?.closeInspect();document.querySelector<HTMLButtonElement>('#inspect-use')!.onclick=()=>game?.acceptInspect();document.querySelector<HTMLButtonElement>('#cancel-effect')!.onclick=()=>game?.cancelEffect();document.querySelectorAll<HTMLButtonElement>('[data-effect]').forEach(button=>button.onclick=()=>game?.useEffect(button.dataset.effect as EffectId));document.querySelector<HTMLInputElement>('#setting-intensity')!.oninput=event=>game?.updateSettings({intensity:Number((event.target as HTMLInputElement).value)/100});(['reduce-motion','limit-sway','disable-shake','disable-bloom'] as const).forEach(name=>document.querySelector<HTMLInputElement>(`#setting-${name}`)!.onchange=event=>{const key=name.replace(/-([a-z])/g,(_,c)=>c.toUpperCase()) as 'reduceMotion'|'limitSway'|'disableShake'|'disableBloom';game?.updateSettings({[key]:(event.target as HTMLInputElement).checked})});}
